# 교수님 미팅 원본 전사록 — 2026-07-03 (금) 16:52, 24분 48초

> 출처: clovanote.naver.com 자동 전사. 참석자 1 = 교수님, 참석자 2 = 학생(본인).
> 이 파일은 RAG grounding source(원문 verbatim)다. 파생 인덱스는 `meeting_0703_actionitems.md`.
> 자동전사라 오탈자·한영전환 오류 다수. 핵심 인용은 타임스탬프로 추적 가능.

---

**03:26 학생** — 저번에 했던 거 처음부터 다시 분석·공부. 다이나믹 섹션 필드 분석부터 다시. 다이나믹 섹션은 16바이트 구조체 배열, 앞 8바이트=태그, 뒤 8바이트=유니온(값 또는 주소). 링커가 elf_dynamic_info 함수에서 다이나믹 배열을 처음부터 끝까지 순회하며 태그 값을 인덱스로 l_info 배열에 주소 저장. → 다이나믹 섹션 = 태그+유니온 16바이트 구조체, l_info = 파싱 인덱스 테이블.

**04:36 학생** — 이번에 한 것: 파싱 인덱스 테이블 래핑 함수 `get-dynamic-info.h` 소스 뜯어봄(공통 수행 지점이라). 흐름: 커널→링커에 PT_INTERP 세그먼트 보고 제어권. 부트스트랩 때 자기 자신도 재배치 안 돼 있어 elf_dynamic_info 호출해 자기 매핑. 재배치 끝난 뒤 dl_main에서 실행 프로그램에 대해 다시 호출.

**05:41 교수님** — 저게 로저 로저 get_dynamic이네.

**05:46 학생** — 부트스트랩은 bootstrap=first로 각 필드를 if문으로 다르게 처리(뒤에서 다룸). DT_NEEDED 의존 라이브러리 들어가서 매핑할 때도 이 함수 호출. → 호출 지점 3곳, 호출 횟수는 라이브러리 개수에 따라 N+2번.

**06:37 교수님** — 세 번 호출되는 거 몰랐네요.

**06:41 학생** — 특성 파악 후 뮤테이션 전략. 첫 특성(교수님이 말씀하셨던 것): **엔트리 중복 시 어떻게 되는지**. 디버그 엔트리는 로더가 대부분 잘 안 읽음. 이걸 스트링 테이블로 조작…

**07:44 학생** — 바꿔봤더니 로더가 뒤에 걸 실제로 읽고, 4만큼 슬라이드된 거였는데 다르게 읽혀서 libc가 날아가 뒤엣것을 읽어 에러. → **중복 DT_ 시 뒤 엔트리를 읽음.** 그리고 재배치 엔트리 크기 지정 엔트리(DT_RELAENT 등)는 assert문 구현이라 24→23 바꾸면 assert 예외처리.

**08:27 교수님** — 이거 얼라인 얘기죠? 엔트리 크기 24. sizeof(Elf) 다르다… l_info 개수랑 relocation 엔트리 개수랑 sizeof 엔트리 개수가 같아야 하는데 달라서 에러. 이거 다 같아야 하나 보네요.

**09:12 학생** — (처리됨) 태그를 바꾸면 **모르는 태그일 경우 무시하고 정상 실행**(디버그를 바꿈).

**09:29 학생** — RUNPATH랑 RPATH 필드, 둘 다 있으면 **RUNPATH만 읽는 경우**.

**09:39~12:40 교수님↔학생** — RUNPATH/RPATH가 런타임 라이브러리 위치(디렉터리 경로). 둘 다 있으면 RUNPATH만 읽고 RPATH 무시. 교수님: "얘를 읽으면 libc.so를 안 읽는다는 얘기." 우선순위 판정 기준 확인 필요. 교수님: **"저게 있으면 런 패스로 하이잭을 할 수 있겠네요."** (RUNPATH 하이재킹)

**12:54 학생** — 엔트리마다 태그 짧은 것/긴 것(버전 관련 태그가 길다). 배열 공간 메모리 최적화를 위해 매크로로 계산 방식이 엔트리마다 다름. 큰 값을 57로 줄여 배열 인덱스로. 큰 값은 태그가 1값인데도 57로, 작은 경우는 태그값 그대로 인덱스.

**14:00~14:29 교수님↔학생** — 산수식(매크로)이 엔트리마다 다름. 버전 관련은 계산 방식이 다 다름. 학생: **"기존 슬롯 인덱스를 조작하면 침범 가능하지 않을까."**

**14:29 교수님 (핵심 지시)** — "정의되지 않은 필드 태그가 있으면 아예 무시하게끔 코드가 되어 있으면, 정의된 필드가 인덱스화되기 전에 필터링되면 공격할 방법이 없잖아요… 정의되지 않은 태그가 무시되니까. **그 전에 무시가 되는지 확인할 필요가 있어요.** 사전에 먼저 필터링된다면 [막힘]. 근데 진짜 봤더니 이거 필터링 안 되네 라고 하면 정말 좋은 방법. 그럼 버전 정보를 우리가 분석할 수 있겠죠."

**15:20~16:13 교수님** — 인덱스화되기 전에 값이 들어올 수 있는지 먼저 봐라. 이미 쓰이는 값들은 겹치지 않게 만들어 놨을 것(안 그랬으면 벌써 문제 났을 것). **정의되지 않은 태그 값이 인덱스로 쓰일 수 있는지 먼저 확인.** 소스 코드 분석하면 좋겠다. LLM에 가능성 검토 시키면 바로 알려줄 것.

**16:13 학생** — 7개 해보다가 엔트리가 많아 할루시네이션 많이 일어나 잘 안 됨.

**16:24~17:58 교수님 (방법론 지시)** — **디버깅(GDB) 걸어서 invalid한 태그 값이 여기까지 오는지 확인.** 또는 instrumentation 넣어 파일에 출력. GDB 붙인 거네요. 설정 초입 부분에 브레이크포인트 잡고 이상한 값 넣은 코드 실행했을 때 그 값이 인덱스 지정하는 데까지 오는지 GDB로 출력. 도달하면 거기서부터 퍼징처럼 계속 넣어볼 수도, 매크로 계산해 넣을 수도. **도달하지 않으면 57 계산 전 훨씬 위에서 필터링. 그거 먼저 봐라.**

**17:58 교수님** — 세이프가드는 클라우드에 신청하면 풀어줄 것.
**18:10 학생** — 예전에 신청해서 했는데 똑같이 걸림.
**18:32 교수님** — 신청했는데도 걸린다. 다시 해보겠다.

**18:44 학생 (핵심 관측)** — 공통 특징: 엔트리에서 **주소 공간을 가리키는 것들은 주소를 바꾸면**, readelf에서 (버전으로 봤을 때) **섹션 헤더를 읽어서 0x50e(51x)로 되고, 0으로 바꿨는데도 다르게 읽히고 실제 실행 시 세그먼트 폴트.**

**19:15~19:52 교수님↔학생** — 교수님: "0인데 왜 0x502는 뭐예요?" 학생: 세그먼트/섹션 헤더에서 바꿔서. 교수님: **"여기는 원래 주소 값이 있어야 하는데 0으로 넣었는데 왜 0x502가 들어갔냐. 섹션(헤더)으로 읽어서."** 교수님: **"섹션 헤더를 읽었다는 얘기는 버전 심볼(심볼 테이블 정보)을 섹션 헤더에서 읽었다는 얘기."**

**20:07~20:46 교수님 (핵심 가설·지시)** — "아니면 그냥 임의의 섹션(을 가리키는 것). **만약에 섹션 헤더의 위치라고 하면 섹션 헤더의 위치도 바꿀 수 있다.** 그리고 **섹션 헤더에 쓰레기를 넣어도 된다(어차피 안 읽으니까). 거기에 우리가 말폼(malform)도 시키면 되겠죠.** 만약에 어떤 조건에 의해서 섹션 헤더 정보가 어드레스(주소로 사용)된다면, 섹션 헤더를 malform시켜서 0이었음에도 버전 심볼에 다른 정보가 읽히겠죠."
**21:12 학생** — 다시 한번 이번에 해보겠습니다. (SHT 폴백 가설 재검증 커밋)

**21:29 교수님 (다음 액션아이템)** —
① **"저번에 얘기했던 것 중에 해시 값 한번 보는 것도 한 번 더 진행."** (GNU_HASH 재확인, 이월 항목)
② "저는 LD를 위주로 봤는데 **골드도 같이 그런 식으로 적용되어 있는지** 본다면 재밌는 실험. RUNPATH가 LD는 이렇게 돼 있는데 **골드는 다르게 접근돼 있다면 LD랑 골드랑 다르게 동작하는 ELF 파일을 만들 수 있어요.** 골드에는 이렇게, LD에는 이렇게 동작. **멀웨어가 골드에서는 동작하고 LD에는 동작 안 하고 도먼트(dormant)** — 숨어 있다가 나오는 거죠." (Gold differential, evasion)

**22:17~22:53 교수님** — 골드는 LLVM 커뮤니티(정정: 실제로는 binutils) 산. 최적화·보안 더 지원할 것, 보통 LD 많이 씀. ELF에 조건 달 수 있을 것 — "기본 LD는 [이렇게]" 메타 정보로.

**23:02 교수님 (사이드 액션아이템)** — "골드까지 되면 [좋고], 그다음에 **아이다(IDA)나 기드라(Ghidra)가 어떻게 인식하는지도 사이드로.** 아마 걔네들도 LD처럼 그대로 안 할 거고 차이가 있을 것. **근접기(분석기)마다 분석 결과가 달라서, 버전 정보가 하나는 최신인데 옛날 버전으로 쓰게끔** 만들 수 있음." (분석기 differential, evasion)

**23:45 학생** — 다음 미팅 언제?
**23:49 교수님** — 다다음 주 초, 화요일 4시. (7/03 기준 → 7/14)

---

## 원문에서 확정되는 교수님 액션아이템 (다음 미팅까지)

| # | 액션아이템 | 근거 타임스탬프 | 성격 |
|---|-----------|----------------|------|
| A | 미정의 태그가 인덱스 계산까지 도달하는지 GDB로 확인 (필터링 전/후) | 14:29, 16:24, 17:58 | 본작업, 방법=GDB |
| B | 섹션헤더(SHT) 폴백 트러스트바운더리 재검증 — 0으로 지운 d_ptr에 SHT 값이 읽히는가 | 18:44, 19:52, 20:07~20:46, 21:12 | 본작업, 학생 커밋 |
| C | GNU_HASH 해시 값 재확인 (이월) | 21:29 | 재오픈 |
| D | Gold 링커 differential — LD↔Gold 다르게 동작하는 ELF (evasion/malware dormant) | 21:38~22:53 | 교수님 제안 |
| E | IDA/Ghidra + 분석기 버전 differential (evasion) | 23:02 | 사이드 |
